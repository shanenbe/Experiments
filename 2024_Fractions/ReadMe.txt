The files

  API_PROVIDER_FOR_WEBPACK_LIB_GENERATION.ts
  BinaryOperatorTrees.ts
  GraphCreatioin.ts
  GraphRepository.ts

are just contained for documentation purposes - they are compiled into lib.js (with the other classes from the experimentation repo).

(take a look there for more details)